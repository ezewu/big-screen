module.exports = {
  plugins: {
    // 'postcss-px-to-viewport': {
    //   viewportWidth: 1920,
    //   viewportHeight: 1080,
    //   unitPrecision: 3,
    //   viewportUnit: 'vw',
    //   // 这里是过滤不转换的css，支持正则，如果框架本身把单位写死支持移动端，
    //   // 可以通过这个过滤掉，比如vux UI框架需要过滤掉
    //   // ['.ignore', '.hairlines', /^\.weui/, /^\.dp/, /^\.scroller/, /^\.ignore/],
    //   selectorBlackList: ['.ignore', '.hairlines', /^\.van/],
    //   minPixelValue: 1,
    //   mediaQuery: false,
    // },
  },
}
